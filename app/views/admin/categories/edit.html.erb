<%= render 'layouts/validates', som: @category %>
<div class= "container">
  <div class= "row">
   <div class= "col">
    <h2>投稿編集</h2>
    <%= form_with model: @category, url: admin_category_path, method: :patch do |f| %>
    <div class="field" >
    <h4>カテゴリ名</h4>
    <%= f.text_field :category_name %>
    </div>
    <div class= "field">
    <%= f.submit 'カテゴリ名の変更' %>
    </div>
    <% end %>
   </div>
  </div>
</div>